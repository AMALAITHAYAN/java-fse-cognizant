# Exercise 4: Arrange-Act-Assert Pattern, Test Fixtures, Setup and Teardown

Scenario: Organize tests using the AAA pattern and use setup and teardown methods.

JUnit 5 uses:
- `@BeforeEach` instead of old JUnit 4 `@Before`
- `@AfterEach` instead of old JUnit 4 `@After`

Run:

```bash
mvn test
```
